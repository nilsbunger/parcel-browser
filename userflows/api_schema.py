# from userflows.models import Profile

#
# class ProfileSchema(ModelSchema):
#
#     class Config:
#         model = Profile
#         # model_fields = "__all__"  # Better not to use "all" to avoid accidental oversharing
#         model_fields = ["user", "phone", "address", "city", "state", "zip"]

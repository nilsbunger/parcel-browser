{
  "name": "my-project",
  "source": "src/index.ts",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "dev": "parcel serve --no-hmr src/*.ts",
    "build": "parcel build"
  },
  "devDependencies": {
    "parcel": "^2.6.2"
  },
  "dependencies": {
    "ol": "^6.14.1"
  }
}
